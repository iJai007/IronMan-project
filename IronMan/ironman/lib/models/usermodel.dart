class UserModel {
  String name, address, area, password;
  int phone, pincode;
  UserModel(
      {required this.name,
      required this.address,
      required this.area,
      required this.phone,
      required this.password,
      required this.pincode});
}
